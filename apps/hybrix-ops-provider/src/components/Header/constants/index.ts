import type { HeaderOption } from '../types';

export const HEADER_OPTION: HeaderOption[] = [
  {
    title: 'Profile',
    menus: [{ title: 'profile', routeName: 'profile', subMenus: null }],
  },
];
